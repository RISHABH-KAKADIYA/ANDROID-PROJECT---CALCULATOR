<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Converter and Calculator"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".converter" />
        <activity android:name=".split" />
        <activity android:name=".percentage" />
        <activity android:name=".discount" />
        <activity android:name=".bmi" />
        <activity android:name=".gst" />
        <activity android:name=".age" />
        <activity android:name=".mortgage" />
        <activity android:name=".currency" />
        <activity android:name=".scientific" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>